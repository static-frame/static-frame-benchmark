
import itertools as it
import numpy as np
from static_frame.core.util import DtypeSpecifier


def dtype_spec_to_element(self, dtype_spec: DtypeSpecifier) -> tp.Any:
    dtype = np.dtype(dtype_spec)

    if dtype.kind

    pass